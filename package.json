{
  "name": "@daaptickets/common",
  "version": "1.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "publish:patch": "npm version patch && npm publish --access public"
  },
  "devDependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^4.1.3"
  }
}
